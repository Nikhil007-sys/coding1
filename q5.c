#include<stdio.h>
int main(){
    // program to calculate volume of cube
    int side;
    printf("enter side of cube;");
    scanf("%d",&side);
    printf("the volume of cube is:%d\n",side*side*side);
    return 0;
}